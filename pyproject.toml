[project]
name = "hackathon"
version = "0.1.0"
description = "Consumers Confidence Indice compute using new"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.3.4",
    "scikit-learn>=1.7.2",
    "torch>=2.9.0",
]
