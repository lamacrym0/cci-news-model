[project]
name = "hackathon"
version = "0.1.0"
description = "Consumers Confidence Indice compute using new"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "polars>=1.34.0",
    "pyarrow>=21.0.0",
    "requests>=2.32.5",
    "torch>=2.9.0",
    "tqdm>=4.67.1",
]
